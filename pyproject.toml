[project]
name = "nbodiesgravity"
version = "1.0.0"
description = "Real-time 3D N-body gravitational simulation in PyQt6 and PyOpenGL"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
